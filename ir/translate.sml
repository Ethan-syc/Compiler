structure Translate =
struct
  type exp = unit
  (* type level = int
  type access = level * Frame.access
  fun newLevel {parent=lev, name=name, formals} = Frame.newFrame(Temp.namedlabel(name), formals) *)
end
